<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.uca.network.core.mapper.attr.StandardattributesMapper">


    <resultMap id="attrMap" type="com.uca.network.core.mapper.attr.dto.StandardattributesDto">
        <result column="id" jdbcType="VARCHAR" property="id"/>
        <result column="description" jdbcType="VARCHAR" property="description"/>
        <result column="resource_type" jdbcType="VARCHAR" property="resourceType"/>
        <result column="created_at" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="updated_at" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>

    <select id="queryAttrsByParam" resultMap="attrMap">
        select t_attr.description,t_attr.id from tbl_standardattributes t_attr where 1 = 1
        <if test="attrids!=null and attrids.size()>0">
            and id in
            <foreach collection="attrids" item="attrId" index="no" open="("
                     separator="," close=")">
                #{attrId}
            </foreach>
        </if>

    </select>

    <insert id="insertStandardattributes" parameterType="com.uca.network.core.mapper.attr.dto.StandardattributesDto">
        insert into tbl_standardattributes
        (
         id,
         description,
         resource_type,
         created_at,
         updated_at
        )
        values
        (
         #{id},
         #{description},
         #{resourceType},
         #{createTime},
         #{updateTime}
        )
    </insert>

    <delete id="deleteStandardattributesById">
        delete from tbl_standardattributes where id =#{id}

    </delete>


    <delete id="deleteStandardattributesBatch" parameterType="java.util.List">
        delete from tbl_standardattributes where 1 = 1
        <if test="ids!=null and ids.size()>0">
            and id in
            <foreach collection="ids" item="id" index="no" open="("
                     separator="," close=")">
                #{id}
            </foreach>
        </if>

    </delete>
</mapper>

